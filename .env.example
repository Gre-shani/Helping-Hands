# Copy to .env in the project root before running docker compose.
# Generate JWT_SECRET with: openssl rand -base64 48

DB_NAME=helping_hands
DB_USERNAME=helping_hands_user
DB_PASSWORD=changeme-use-a-real-password

JWT_SECRET=replace-with-output-of-openssl-rand-base64-48
